# WASM Modules

Copy compiled WASM binaries here:

```bash
cp ~/EdgeAgent/wasm_mcp/target/wasm32-wasip2/release/mcp_server_filesystem.wasm ./
cp ~/EdgeAgent/wasm_mcp/target/wasm32-wasip2/release/mcp_server_filesystem_http.wasm ./
```
